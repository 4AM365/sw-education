git init

.gitignore

git push -u origin master

what's src refspec master?

git a



Files are 'tracked' when I commit them
If I add a gitignore or get rid of things, I need to remove them from git

i.e. git rm --cached dist/ blah blah blah.

Commit to doing the change git commit -m "note"

Then git push it out